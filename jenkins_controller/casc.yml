jenkins:
  numExecutors: 0
  securityRealm:
    local:
      users:
      - id: "panda"
        name: "panda"
        password: "panda"
  authorizationStrategy:
    globalMatrix:
      permissions:
      - "Overall/Administer:panda"
  credentials:
    system:
      domainCredentials:
        - credentials:
          - usernamePassword:
            id: "artifactory"
            username: "admin"
            password: "password"
            scope: "GLOBAL" 
